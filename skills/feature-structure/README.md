## 🚀 Usage

To apply this structure to a new feature named `foo`:

```bash
npx skills add Juliotati/project-skills --skill feature-structure
```

Prompt Example:

> "Create a new feature called 'Profile' using the feature-structure skill. Use Provider for state management and include a User model."
