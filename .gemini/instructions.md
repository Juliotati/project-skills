Whenever you need to generate a script to perform a task or do something, you MUST ALWAYS use Dart instead of Python or any other language. Dart must be the language of choice for any scripting tasks.

When editing Dart code, you MUST ALWAYS use the `code-quality` skill.
